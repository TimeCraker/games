
--- WIKI PAGE: #-📆-2026.03.03.02-下午开发笔记-A.md ---

---

# 📆 2026.03.03.02 下午开发笔记 A

## 主题：实现玩家位置同步逻辑，升级 WebSocket 为 JSON 协议分流处理器

### ✅ 完成工作

1. **协议升级**  
   - 将 WebSocket 通信从纯文本升级为 **JSON 格式**，引入 `GameMessage` 结构体，包含 `type` 字段区分指令（`chat` / `move`）。

2. **消息分流处理**  
   - 在 `HandleWS` 循环中解析 JSON，根据 `type` 调用不同业务逻辑：
     - `chat` → `handleChatLogic`
     - `move` → `handleMoveLogic`

3. **聊天消息持久化与广播**  
   - `handleChatLogic`：将消息存入 `models.Message` 表，并向全服广播 JSON 格式的聊天内容。

4. **玩家位置同步**  
   - `handleMoveLogic`：更新或创建 `models.PlayerPosition` 记录（坐标 X/Y/Z），并向全服广播位置更新 JSON。

5. **广播标准化**  
   - 所有广播数据均为 JSON 格式，方便前端（Unity/网页）直接解析并驱动模型移动或显示聊天。

### 🧪 测试方法
- 启动服务，用 Postman 连接 WebSocket。
- 发送 JSON 格式消息：
  ```json
  {"type":"move","x":10.5,"y":0,"z":20.0}
  {"type":"chat","content":"hello"}
  ```
- 观察数据库 `player_positions` 表是否更新，另一客户端是否收到广播。

### 🧠 技术要点
- **协议设计**：统一的 JSON 结构使后端可扩展更多指令类型（如 `attack`、`join_room`）。
- **数据库同步**：玩家位置实时落盘，支持断线重连后恢复坐标。
- **广播格式**：包含 `type` 字段，前端可针对不同消息渲染不同效果。

---

**下一步**：可考虑引入 Redis 缓存热点位置数据，降低 MySQL 压力。

--- WIKI PAGE: #-📆-2026.03.03.04-晚上-开发笔记.md ---

---

# 📆 2026.03.03 晚上开发笔记

## 🎯 主题：心跳机制 + Protobuf 协议升级准备

### ✅ 今日新增功能
- **心跳保活**：后续将集成 WebSocket 心跳（Ping/Pong），自动检测并清理僵尸连接。
- **聊天记录回溯**：新玩家上线时可拉取最近的历史消息（待实现）。

### 🔄 协议升级：从 JSON 到 Protobuf
#### 为什么换？
- **性能**：二进制解析比 JSON 快 10–100 倍，CPU 开销低。
- **体积**：同样数据体积缩小 5–10 倍，节省带宽。
- **严谨**：强类型定义，避免字段错漏。

#### 操作步骤
1. **安装工具**：
   - 下载 `protoc` 编译器，配置环境变量。
   - 安装 Go 插件：`go install google.golang.org/protobuf/cmd/protoc-gen-go@latest`
2. **编写协议文件**：在 `services/auth/proto/game.proto` 定义 `GameMessage` 等消息结构。
3. **生成 Go 代码**：执行 `protoc --go_out=. game.proto`，得到 `game.pb.go`。

### 📌 Git Commit 建议
```bash
git commit -m "feat: implement websocket heartbeat mechanism and chat history playback

- Prepare for Protobuf migration by defining proto schemas
- Heartbeat mechanism to detect dead connections (to be integrated)
- Chat history retrieval for new players (pending implementation)"
```

### ⏭️ 下一步
- 安装配置好 Protobuf 环境。
- 修改 `websocket.go`，用 `proto.Unmarshal` 替换 `json.Unmarshal`，全面升级为二进制通信。

---

--- WIKI PAGE: #-📆-2026.03.11-开发笔记-晚上.md ---

# 📆 2026.03.11 开发笔记

## ✅ 今日改动清单

### 1. 数据模型层（`models/user.go`）

- **修改内容**：在 `User` 结构体中新增 `Email` 字段，并添加 `uniqueIndex` 约束。
- **修改原因**：支持玩家通过邮箱找回账号或进行身份验证，同时确保一个邮箱只能注册一个账号。
- **功能实现**：GORM 自动迁移（`AutoMigrate`）会在数据库中自动生成 `email` 列并创建唯一索引。

### 2. 工具层（`utils/email.go`）

- **修改内容**：实现基于 SMTP 协议（TLS/465 端口）的邮件发送函数，并注入 HTML/CSS UI 模板。
- **修改原因**：提升玩家注册体验，同时通过标准化的 SMTP 头部格式（`From`、`To`、`\r\n`）解决 QQ 邮箱的屏蔽问题。
- **功能实现**：封装底层 SMTP 发送逻辑，支持发送蓝白配色卡片式验证码邮件。

### 3. 业务逻辑层（`handlers/`）

- **`register.go`**  
  - 修改注册请求结构，新增 Redis 验证码比对逻辑。  
  - 注册前先查询 Redis 中的验证码，对不上直接拒绝，成功后立即销毁验证码防止复用。

- **`send_code.go`（或 `email.go`）**  
  - 实现随机 6 位验证码生成。  
  - 将验证码存入 Redis 并设置 5 分钟有效期，随后调用 `utils/email` 发送邮件。

### 4. 入口层（`main.go`）

- **修改内容**：引入 `v1 := r.Group("/api/v1")` 路由分组。
- **修改原因**：遵循 API 版本化管理规范，将 `register`、`login` 和 `send-code` 统一挂载。
- **功能实现**：对外暴露标准的 `/api/v1/send-code` 接口。

## 📌 Git 提交信息

```bash
git add .
git commit -m "feat: 实现带 HTML 模板的邮箱验证码系统及 v1 路由规范" -m "1. 升级 user 模型增加 Email 唯一字段
2. 实现 utils/email 支持 TLS 安全发送及精美 HTML UI 模板
3. register 接口增加 Redis 验证码校验逻辑与销毁机制
4. main.go 重构为 api/v1 分组管理模式"
```

## 🧠 技术要点总结

- **邮箱唯一性**：通过数据库唯一索引保证，防止重复注册。
- **验证码安全**：使用 Redis 存储验证码并设置 5 分钟有效期，发送前校验、使用后销毁，防止暴力破解和复用。
- **限流隐式实现**：结合 Redis 的过期特性，可后续扩展发送频率限制。
- **路由版本化**：`/api/v1` 为未来接口演进提供清晰路径，同时保持向后兼容。

## 🚀 当前项目状态

- 注册流程已完善，支持邮箱验证码验证，符合生产级安全规范。
- 邮件发送功能独立封装，便于后续更换邮件服务商或扩展模板。
- 接口结构清晰，认证与业务路由分离，为后续功能扩展打下基础。

--- WIKI PAGE: #-📆-2026.03.11-开发笔记.md ---

# 📆 2026.03.11 开发笔记

## ✅ 今日完成

### 1. 修复世界状态同步中的幽灵玩家问题

- **问题**：新玩家加入时，服务器会将已离线玩家的历史位置数据（仍存于数据库）同步给新客户端，导致出现“幽灵玩家”。
- **解决方案**：重构 `websocket.go` 中的 `syncWorldState` 逻辑，改为遍历 `GlobalHub` 中的**活跃 WebSocket 连接**，只将当前在线玩家的位置同步给新客户端。
- **效果**：彻底杜绝离线玩家残留数据被错误同步，保持场景纯净。

### 2. 路由重构与邮箱验证码功能

#### 路由结构调整（`main.go`）

- 恢复被误删的 `/api` 路由组（`AuthMiddleware`）及其 `/me` 接口。
- 建立版本化路由 `/api/v1`，将注册、登录接口迁移至此。
- 新增 `/api/v1/send-code` 接口，用于发送邮箱验证码。

#### 数据模型扩展（`models/user.go`）

- 为 `User` 模型新增 `Email` 字段，并添加唯一索引约束，确保邮箱全局唯一。

#### 注册流程强化（`handlers/register.go`）

- 注册接口增加邮箱冲突检查，防止重复注册。
- 强制校验邮箱验证码：客户端必须提供 5 分钟内有效的 6 位验证码，验证码由 Redis 存储和管理。

#### 验证码发送与限流（`handlers/email.go`）

- 实现基于 Redis 的验证码发送限流逻辑：
  - 同一邮箱 **60 秒内**不能重复发送。
  - 同一邮箱 **每日最多发送 5 次**。
- 验证码有效期为 5 分钟，过期自动失效。

#### 邮件发送工具（`utils/email.go`）

- 封装底层 SMTP 发送逻辑，支持真实邮件投递。
- 与业务逻辑解耦，便于后续复用或更换邮件服务商。

## 📌 Git 提交信息

### 后端幽灵玩家修复
```bash
fix(backend): resolve ghost player issue in world state sync

- Refactor syncWorldState to iterate over active WebSocket connections in GlobalHub
- Prevent offline players' stale position data from being sent to new clients
```

### 路由重构与邮箱验证码功能
```bash
feat: restructure routing to v1 and introduce email verification

- Restore /api auth group and /me endpoint in main.go
- Create /api/v1 group and migrate register/login endpoints
- Add Email field with unique index to User model
- Enhance register flow with Redis-based email code validation (60s/5 daily limit)
- Implement SMTP email sending utility in utils/email.go
```

## 🧠 技术要点总结

- **世界状态同步**：应基于实时连接池而非数据库历史记录，避免幽灵玩家。
- **版本化路由**：`/api/v1` 便于未来接口演进，同时保持旧路由兼容。
- **邮箱验证码**：结合 Redis 实现限流和过期控制，提升注册安全性和防滥用能力。
- **最小修改原则**：所有改动尽量保留原有注释和结构，确保代码可维护性。

## 🚀 当前项目状态

- 玩家加入场景时只能看到真正在线的人，世界状态准确。
- 注册流程已完善，支持邮箱验证码验证，符合生产级应用规范。
- 后端接口结构清晰，认证与业务路由分离，为后续功能扩展打下基础。

--- WIKI PAGE: #-📆-2026.03.13-上午开发笔记：项目架构重构与工程规范化.md ---

# 📆 2026.03.13 上午开发笔记：项目架构重构与工程规范化

## ✅ 重构内容概览

本次重构以微服务化和工程规范化为目标，对项目布局进行了系统性调整，主要涉及以下五个方面：

1. **统一模块管理**  
   - 将 `go.mod` 从子目录移至**项目根目录**，建立单一 Module 结构，使 `auth`、`gateway` 等服务能互相引用代码，无需发包到云端。

2. **按业务域拆分 handlers**  
   - 将原先庞大的 `handlers` 文件夹按业务域拆分为独立子包：`account`、`send_email` 等，为后续添加商城、背包等功能提供清晰扩展路径。

3. **抽离长连接服务**  
   - 将 WebSocket 和 Hub 相关逻辑迁移至独立的 `gateway` 服务目录，实现连接管理与业务逻辑的物理分离。

4. **共享 Protobuf 定义**  
   - 将 Protobuf 文件及生成的 Go 代码移至项目级共享 `proto` 目录，方便 `auth` 和 `gateway` 服务统一调用，避免重复定义。

5. **消除隐式全局状态**  
   - 彻底移除之前在各 handler 中透传的全局 `DB` 变量，全部替换为明确的 `db.SQLDB` 调用，使数据流向清晰可控，预防并发问题。

6. **修复依赖与路由**  
   - 修复所有因目录调整导致的 import 路径错误，并更新 `main.go` 中的路由注册，确保服务可正常启动。

## 📌 Git 提交信息（中英对照）

在终端执行以下命令（或使用编辑器提交）：

```bash
git add .
git commit -m "refactor: restructure project layout and fix global dependencies" -m "Details:
- Relocate 'go.mod' to project root for unified module management.
- Extract handlers into domain-driven sub-packages (account, send_email).
- Migrate WebSocket and Hub components to a standalone 'gateway' service directory.
- Move Protobuf files to a shared 'proto' directory for cross-service access.
- Completely remove the deprecated global 'DB' variable, replacing it with explicit 'db.SQLDB' calls.
- Fix all cross-package import paths and update router bindings in 'main.go'.

中英对照说明：
- 将 go.mod 移至项目根目录，确立单一 Module 统一管理。
- 按业务域将 handlers 拆分为 account、send_email 等独立子包。
- 将长连接相关逻辑（WebSocket、Hub）抽离至独立的 gateway 服务目录。
- 将 Protobuf 代码移至共享 proto 目录，方便跨服务调用。
- 彻底移除失效的隐式全局 DB 变量，全部替换为明确的 db.SQLDB 调用。
- 修复所有因目录调整导致的 import 路径错误，并更新 main.go 的路由注册。"
```

## 🧠 架构意义解读

- **单一 Module 结构**：项目成为完整的 Monorepo（大仓库），所有微服务（`auth`、`gateway`）均可直接引用内部代码，无需推送到外部包管理仓库，大幅简化开发调试。
- **按业务域分包**：遵循领域驱动设计原则，代码按功能模块自然分离，新增功能只需创建对应包，避免 `handlers` 文件夹无限膨胀。
- **服务边界清晰**：将长连接逻辑抽离为独立 `gateway` 服务，为后续独立部署、水平扩展打下基础。
- **共享协议定义**：统一 `proto` 目录确保 `auth` 与 `gateway` 对消息结构的理解完全一致，消除因定义副本不同步引发的隐蔽 Bug。
- **消除全局状态**：抛弃隐式 `DB` 变量，强制通过显式依赖传递，数据流向透明，为并发安全和单元测试提供保障。

本次重构使项目结构更贴近生产级微服务标准，为后续功能迭代和团队协作奠定了坚实基础。

--- WIKI PAGE: #-📆-2026.03.13-开发笔记：Unity-前端-UI-重构与-SPA-风格认证流程.md ---

# 📆 2026.03.13 开发笔记：Unity 前端 UI 重构与 SPA 风格认证流程

## ✅ 主要功能实现

### Git 提交信息
```bash
feat(ui): implement SPA-style auth flow with UI Toolkit

- 使用 UI Toolkit（UXML/USS）替换旧版 UGUI，构建组件化 UI 架构，类似 Vue/React 单页面应用。
- 引入 DataManager 用于全局存储 JWT Token 和 UserID。
- 添加 HttpManager 处理 RESTful API 通信（注册、验证码、登录）。
- 实现 LoginController 控制登录/注册面板的 SPA 风格无缝切换。
- 修复 NetManager 在 WebSocket 连接建立前处理输入的逻辑缺陷。
- 将 3D 环境渲染延迟至认证成功后执行，确保游戏逻辑与登录界面隔离。
```

## 🧠 架构变更详解

### 1. UI 层全面升级
- **抛弃 UGUI**，全面采用 **UI Toolkit**（Unity 的新 UI 系统）。
- 使用 **UXML** 定义结构化的界面组件，**USS** 统一管理样式，实现界面与逻辑分离。
- `LoginController.cs` 控制视图切换和事件绑定，实现登录/注册面板的无缝切换，体验类似 SPA 前端框架。

### 2. 数据管理层
- `DataManager.cs` 作为静态类，负责缓存用户认证后的 Token 和 UserID，供全局访问。

### 3. 网络层优化
- `HttpManager.cs` 负责所有 RESTful API 调用（注册、发送验证码、登录），统一处理请求与响应。
- `NetManager.cs` 修复了关键逻辑：在 WebSocket 连接成功前禁止处理输入，确保游戏交互仅在认证通过后触发。

### 4. 场景控制
- 将 3D 游戏环境的渲染延迟到用户成功认证之后，避免未登录玩家看到游戏世界，提升安全性和体验。

## 🔥 核心踩坑记录（必读）

### 1. XML 严格模式
- 修改 `LoginView.uxml` 时，注释中**绝对不能包含 `--`**，否则 Unity 解析器会崩溃。
- `<ui:TextField>` 绑定默认值必须使用 `value="..."`，**不能用 `text="..."`**。

### 2. UI Toolkit 字体挂载
- Unity 2022+ 对字体区分严格，**不能使用传统 TMP Font Asset**。
- 必须通过 `Create -> Text -> Font Asset` 生成专属字体。
- 在 Inspector 的 `Text` 栏中，必须 **清空（Unset）默认的 `Font` 槽位**，只将生成的资产拖入 `Font Asset` 槽位，否则会冲突或无法拖拽。

### 3. CSS 布局塌陷
- 在 `LoginStyles.uss` 中，最外层背景必须设置：
  ```css
  position: absolute;
  width: 100%;
  height: 100%;
  ```
  否则 Unity 会将其压缩为 `0x0`，导致背景不可见。

### 4. 网络状态拦截
- `NetManager.cs` 的 `Update` 循环已加入：
  ```csharp
  if (_socket == null || _socket.State != WebSocketState.Open) return;
  ```
- **后续添加任何游戏输入逻辑时，必须确保在网络连接成功后执行**，防止在登录界面触发 3D 场景交互。

## 🚀 当前状态

- **前端认证闭环**：登录/注册流程完整打通，UI 响应式切换流畅，数据存储与网络请求分离清晰。
- **后端匹配**：后端（Go）仍运行在 `localhost:8081`，API 路由为 `/api/v1`，WebSocket 为 `/ws`，支持 JWT 鉴权与 Protobuf 通信。
- **场景隔离**：3D 环境仅在认证成功后渲染，符合产品级体验规范。

## 📌 下一步开发建议

### 后端
- 可开始编写 **Match 匹配服务**，或进一步完善 Redis 状态同步逻辑。
- 考虑将认证服务与网关服务分离，推动微服务化。

### 前端
- 构建 **游戏大厅 UI**（房间列表、玩家状态）。
- 优化其他玩家移动的网络插值算法，消除轻微卡顿，提升同步平滑度。

--- WIKI PAGE: 2026.03.04-开发笔记.md ---

---

# 2026.03.04 开发笔记：Protobuf迁移与Unity客户端对接

## ✅ 今日完成
- **协议二进制化**：WebSocket 从 JSON 迁移至 Protobuf，性能提升（体积更小、解析更快）。
- **双向通信验证**：客户端发送 `move` 指令，服务器返回历史/初始化数据，交互正常。
- **心跳保障**：长连接稳定性增强，僵尸连接自动清理。

## 🎮 Unity 客户端对接（Demo 可视化）
### 环境配置
1. 将 `Google.Protobuf.dll` 放入 Unity `Plugins` 文件夹。
2. 用 `protoc` 将 `game.proto` 生成 C# 版本（`game.cs`），存入 `Proto` 文件夹。
3. 安装 Visual Studio 2022 用于 C# 开发。

### 核心脚本 `NetManager`
- 位置：`Assets/Scripts/Network/NetManager.cs`
- 功能：
  - 连接 Go 后端 WebSocket（`ws://localhost:8081/ws?token=...`）。
  - 接收二进制 Protobuf 消息并解析。
  - 发送测试移动数据（模拟坐标）。

### 测试步骤
1. 启动 Go 后端（`go run main.go`）。
2. 运行 Unity 场景，观察控制台输出 `✅ 已成功连接到 Go 后端服务器！`。
3. 调用 `SendTestMove()` 发送坐标，后端应广播位置更新，其他客户端同步显示。

## 📌 Git 提交信息
```bash
feat: migrate websocket protocol to Protobuf and structure test suite
```

---

--- WIKI PAGE: 2026.03.10-开发笔记：断线清理与平滑移动优化.md ---

# 📆 2026.03.10 开发笔记：断线清理与平滑移动优化

## ✅ 今日完成

### 1. **前端（Unity）优化**
- **断线清理**：增加了 `logout` 消息处理，玩家断开连接时销毁对应的游戏对象，彻底解决了“幽灵方块”残留问题。
- **逻辑解耦**：确保 `logout` 事件独立处理，不与移动逻辑嵌套。
- **简化行为**：移除了冗余的 `LookAt` 转向逻辑，使方块行为更加纯粹。
- **协议对齐**：保留功能注释，确保消息类型与后端 Protobuf 定义一致。

### 2. **后端（Go）协议同步**
- **事件类型统一**：在 WebSocket 断开清理时广播 `logout` 消息，而非 `leave`，确保与客户端处理器匹配。
- **消息类型一致性**：确保 Protobuf 定义的 `logout` 类型与 C# 客户端严格对应。
- **日志优化**：改进了连接释放时的日志输出，便于服务端调试。

## 📌 **Git 提交信息**

### **Unity 前端（NetManager.cs）**
```bash
feat(network): implement logout cleanup and optimize movement

- Added 'logout' message handling to destroy player objects upon disconnection.
- Fixed logic nesting to ensure logout events are processed independently of movement.
- Removed redundant 'LookAt' rotation logic for simplified cube behavior.
- Retained all functional comments and synchronized protocol types with backend.
Go 后端（websocket.go）
fix(api): synchronize disconnection event type with client protocol

- Updated WebSocket defer cleanup to broadcast "logout" instead of "leave".
- Ensured consistency between Protobuf message types and C# client handlers.
- Optimized connection release logging for better server-side debugging.


### 🧠 技术要点总结

平滑移动：已完全实现，所有方块均能平滑滑行，消除瞬移感。

身份区分：根据玩家 ID 自动变色（白色为本地玩家，彩色为其他玩家），视觉效果清晰。

断线清理：幽灵方块彻底消失，场景始终保持干净。

协议闭环：前后端 Protobuf 通讯类型完全对齐，为后续功能扩展奠定了基础。

###🚀 当前项目状态

多客户端同步稳定运行，支持平滑移动、断线清理、身份识别。

后端稳定广播坐标，前端准确解析渲染。

所有核心功能闭环，可交付 Demo 演示

--- WIKI PAGE: Home.md ---

Welcome to the game-backend-demo wiki!

--- WIKI PAGE: 联机对战游戏后端实战项目规划.md ---

# 🎮 联机对战游戏后端实战项目规划 (2026 版)

## 0. 项目背景与目标

本项目旨在从零搭建一个基于 **Go + WebSocket + Unity** 的服务端权威（Server Authoritative）联机对战 Demo。

* **核心目标**：突破传统 CRUD 局限，掌握长连接管理、状态同步、分布式缓存及高性能网络架构。
* **求职定位**：针对 2026 秋招，对标一线厂（米哈游、腾讯、网易）游戏后端/高性能开发岗位。

---

## 🏗️ 整体架构图

---

## 📅 路线图 (Milestones)

### 🚩 第一阶段：基础架构与账号系统 (Week 1-2) —— **当前阶段**

* **[Done]** 容器化环境搭建 (Docker: MySQL, Redis)。
* **[Done]** 用户系统 (Auth)：基于 Gin 框架，支持 Bcrypt 密码哈希存储。
* **[Done]** 鉴权体系：实现 JWT 签发与鉴权中间件。
* **[ToDo]** 状态感知：集成 Redis，实现用户在线状态维护与过期策略。

### 🚩 第二阶段：长连接网关与消息协议 (Week 3-4)

* **核心技术**：WebSocket (gorilla/websocket)。
* **任务清单**：
1. **Gateway 服务**：实现从 HTTP 到 WS 的协议升级。
2. **连接管理**：维护全局连接池（Map/Sync.Map），处理玩家异常掉线。
3. **心跳机制**：双向心跳（Ping/Pong），清理“僵尸连接”。
4. **消息路由**：定义基础消息包结构 (Header+Body)，将消息分发至对应逻辑模块。



### 🚩 第三阶段：房间系统与匹配逻辑 (Week 5-6)

* **核心技术**：并发控制 (Mutex/Channel)。
* **任务清单**：
1. **房间管理**：Create / Join / Leave 逻辑实现。
2. **简单匹配**：基于队列（Queue）实现的先到先得匹配机制。
3. **广播系统**：向房间内所有玩家同步“玩家进入”、“游戏开始”等事件。



### 🚩 第四阶段：服务端权威战斗同步 (Week 7-9) —— **核心难点**

* **核心技术**：Tick-Based Loop, 状态快照 (Snapshot)。
* **任务清单**：
1. **Tick 驱动**：服务端开启 20Hz (50ms/tick) 循环逻辑。
2. **状态模拟**：服务端计算位移、碰撞，防止客户端作弊。
3. **快照同步**：每 Tick 打包全量/增量状态，广播给所有客户端。
4. **网络优化**：尝试实现基础的插值 (Interpolation) 数据准备。



### 🚩 第五阶段：Unity 客户端对接 (Week 10-11)

* **核心技术**：Unity C#, Mirror/Netcode 原理。
* **任务清单**：
1. **通信层**：C# 实现 WebSocket 客户端逻辑。
2. **表现层**：将服务端下发的坐标映射到 3D 胶囊体/模型上。
3. **平滑处理**：在客户端实现位置插值，消除网络抖动带来的卡顿。



### 🚩 第六阶段：工程化压测与亮点打磨 (Week 12+)

* **核心技术**：性能分析 (Pprof), 压力测试。
* **任务清单**：
1. **分布式链路**：使用 Prometheus 监控服务健康度。
2. **高并发压测**：编写脚本模拟 500+ 同时在线，记录 CPU/内存占用。
3. **简历包装**：产出架构文档、压测报告，准备面试话术。



---

## 🛠️ 技术栈清单 (Stack)

* **语言**：Golang (Go 1.25+)
* **Web 框架**：Gin
* **数据库**：MySQL 8.0 (GORM)
* **缓存/状态**：Redis 7.0 (go-redis)
* **长连接**：WebSocket
* **容器化**：Docker / Docker-Compose
* **客户端**：Unity 2022+

---

## 亮点

1. **“自研服务端权威同步引擎”**：避开简单 CRUD，直接挑战游戏开发最难的同步一致性问题。
2. **“高性能连接池管理”**：通过千万级长连接管理优化，展现对 Go 并发模型的底层理解。
3. **“Redis 在线状态分发”**：利用缓存提升响应速度，降低主库压力。

---

### 💡 如何使用本 Wiki？

* **开发前**：确认当前所属阶段，查看任务清单。
* **开发中**：每完成一个子任务，在 Wiki 中勾选。
* **开发后**：将本周遇到的 Bug 和解决方案记录在 Wiki 底部的 `Dev Log`。

---

--- WIKI PAGE: 📆-2026.02.26-极简笔记.md ---

# 📆 2026.02.26 极简笔记（省流版 + 补充说明）

## ✅ 今日完成
- **代理环境**：从 GitHub 找到 Clash 内核，购买飞鸟云机场（10.5 元 / 200G），配置后可访问 ChatGPT、Gemini（主力用 Gemini 免费额度）。
- **仓库调整**：
  - 修改仓库名称（具体未说明，但后续目录结构已体现）。
  - 重构项目目录为标准布局：
    ```
    my-backend-demo/
    ├── services/       # 存放所有 Go 微服务
    │   ├── auth/       # 认证服务
    │   ├── gateway/    # 网关服务
    │   └── match/      # 匹配服务
    ├── scripts/        # 一键启动/压测等脚本（.ps1）
    ├── docs/           # 文档、架构图
    └── .gitignore      # 忽略规则
    ```
  - 已 `git add` / `commit` / `push` 到 GitHub。
- **Docker 环境搭建**：
  - 编写 `docker-compose.yml`，内容如下：
    ```yaml
    version: '3.8'
    services:
      mysql:
        image: mysql:8.0
        container_name: game-mysql
        environment:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: game_dev
        ports:
          - "3306:3306"
        volumes:
          - mysql-data:/var/lib/mysql
        restart: unless-stopped
      redis:
        image: redis:7
        container_name: game-redis
        ports:
          - "6379:6379"
        volumes:
          - redis-data:/data
        restart: unless-stopped
    volumes:
      mysql-data:
      redis-data:
    ```
  - 运行 `docker-compose up -d`：自动拉取 MySQL 8.0 和 Redis 7 镜像，创建并后台启动两个容器。
  - 在 `scripts/` 中添加了三个 PowerShell 脚本（.ps1），用于封装常用命令（例如启动/停止容器、查看日志等）。

---

## 📚 补充说明

### 1. Docker Compose 是什么？
- `docker-compose.yml` 是一个定义多容器应用的工具。你写的这个文件定义了两个服务（`mysql` 和 `redis`），以及它们需要的配置（密码、端口、数据卷等）。
- `docker-compose up -d` 命令会：
  - 如果本地没有指定的镜像，自动从 Docker Hub 拉取。
  - 根据配置创建并启动容器，`-d` 表示后台运行。
  - 创建两个数据卷（`mysql-data` 和 `redis-data`），用于持久化存储数据库文件，即使容器删除数据也不会丢失。

### 2. MySQL 和 Redis 的角色
- **MySQL**：关系型数据库，用于存储结构化数据（如用户信息、游戏记录等）。你设置了 root 密码 `rootpassword` 和初始数据库 `game_dev`，方便后续开发。
- **Redis**：**不是日志记录工具**，而是一个内存中的键值存储系统，常用作**缓存**、**消息队列**或**临时数据存储**（比如存储用户 session、排行榜等）。它基于内存，读写极快，但数据默认不持久化（你挂载了数据卷 `redis-data` 到 `/data`，可以持久化）。

### 3. 脚本文件（.ps1）的作用
- PowerShell 脚本（`.ps1`）可以将常用命令封装起来，例如：
  - `start.ps1`：`docker-compose up -d`
  - `stop.ps1`：`docker-compose down`
  - `logs.ps1`：`docker-compose logs -f`
- 这样你就不用每次手动输入长命令，直接双击或 `.\start.ps1` 即可。


## 💡 下一步建议
1. **测试连接**：确保 MySQL 和 Redis 容器正常运行，可以用以下命令进入容器测试：
   ```bash
   docker exec -it game-mysql mysql -uroot -prootpassword
   docker exec -it game-redis redis-cli
   ```
2. **完善 `.gitignore`**：添加 Docker 相关临时文件（如 `.env` 如果存放密码，不应提交）。
3. **开始编写微服务**：在 `services/auth/` 等目录下写 Go 代码，并用 Docker 网络让容器互通（默认 Compose 会创建网络，服务名可直接作为主机名）。
4. **记录常用命令**：把每天用的命令记入 `scripts/`，不断积累。

---

## 📝 今日总结
今天搭建了代理环境，重构了项目结构，并成功用 Docker Compose 启动了 MySQL 和 Redis，为后端微服务准备好了基础设施。现在可以随时使用 `docker-compose up -d` 一键启动数据库，并用脚本简化操作。继续保持！

明天开始写第一个微服务（例如 auth 服务），并尝试连接 MySQL。

--- WIKI PAGE: 📆-2026.02.27-开发笔记.md ---

# 📆 2026.02.27 开发笔记（Auth 服务核心功能实现）

## ✅ 今日完成工作概览

- **完善脚本**：在 `scripts/` 中编写了三个 PowerShell 脚本，用于一键管理本地 Docker 环境。
- **创建 Auth 微服务**：在 `services/auth` 目录下初始化 Go 模块，并实现用户注册、登录、鉴权等基础功能。
- **工程化重构**：根据 Gemini 建议引入 Gin 框架，并按照分层结构组织代码（handlers、models、utils、middleware）。
- **数据库集成**：使用 GORM 连接 MySQL，并自动迁移 User 模型；连接 Redis 用于后续缓存。
- **接口测试**：通过 curl 命令测试健康检查、注册、登录及鉴权接口。

---

## 📁 项目结构更新（services/auth）

```
services/auth/
├── main.go
├── go.mod
├── go.sum
├── models/
│   └── user.go               # 用户数据模型
├── handlers/
│   ├── register.go           # 注册接口
│   ├── login.go              # 登录接口（生成 JWT）
│   └── user.go               # 获取当前用户信息
├── middleware/
│   └── auth.go               # JWT 鉴权中间件
├── utils/
│   └── jwt.go                 # JWT 生成与解析工具
└── (后续可添加 config/、routes/ 等)
```

---

## ⚙️ 环境管理脚本（scripts/）

| 脚本文件 | 命令 | 作用 |
|----------|------|------|
| `local_up.ps1` | `docker-compose up -d` | 后台启动 MySQL 和 Redis 容器 |
| `local_down.ps1` | `docker-compose down` | 停止并清理容器、网络 |
| `local_logs.ps1` | `docker-compose logs -f` | 实时查看容器日志（调试用） |

使用方式：在 `my-backend-demo/` 目录下执行 `.\scripts\local_up.ps1` 即可一键拉起环境。

---

## 🚀 Auth 服务开发步骤

### 1. 初始化模块与安装依赖
```bash
cd services/auth
go mod init github.com/TimeCraker/game-backend-demo/services/auth
go get -u gorm.io/driver/mysql
go get -u github.com/redis/go-redis/v9
go get -u github.com/gin-gonic/gin
go get -u github.com/golang-jwt/jwt/v5
```

### 2. 定义用户模型（models/user.go）
```go
package models

import "gorm.io/gorm"

type User struct {
    gorm.Model
    Username string `gorm:"uniqueIndex;size:50;not null"`
    Password string `gorm:"size:255;not null"` // 存储加密后的密码
}
```

### 3. 编写 JWT 工具（utils/jwt.go）
- 包含生成 Token 和解析 Token 的函数。
- 使用密钥（可从环境变量读取，暂硬编码为示例）。
- 用户 ID 存入 Token 的 Claims 中。

### 4. 编写中间件（middleware/auth.go）
- 从请求头 `Authorization: Bearer <token>` 中提取 JWT。
- 验证 Token 合法性，将用户 ID 存入 Gin 上下文（`c.Set("userID", uid)`）。
- 验证失败返回 401。

### 5. 编写处理函数（handlers/）

#### register.go
- 绑定 JSON 请求体到结构体（用户名、密码）。
- 密码加密（使用 `bcrypt` 或后续改进）。
- 调用 GORM 创建用户。
- 返回成功或错误信息。

#### login.go
- 绑定 JSON 请求体。
- 根据用户名查询用户，比对密码（加密后比对）。
- 生成 JWT Token 并返回。

#### user.go（对应 `/api/me`）
- 从上下文中获取用户 ID（由中间件设置）。
- 查询数据库返回用户信息（不含密码）。

### 6. 重构 main.go（使用 Gin）
```go
package main

import (
    "log"
    "github.com/gin-gonic/gin"
    "github.com/TimeCraker/game-backend-demo/services/auth/handlers"
    "github.com/TimeCraker/game-backend-demo/services/auth/middleware"
    "github.com/TimeCraker/game-backend-demo/services/auth/models"
    "github.com/redis/go-redis/v9"
    "gorm.io/driver/mysql"
    "gorm.io/gorm"
)

var (
    db  *gorm.DB
    rdb *redis.Client
)

func main() {
    // 连接 MySQL
    dsn := "root:rootpassword@tcp(localhost:3306)/game_dev?charset=utf8mb4&parseTime=True&loc=Local"
    var err error
    db, err = gorm.Open(mysql.Open(dsn), &gorm.Config{})
    if err != nil {
        log.Fatalf("MySQL连接失败: %v", err)
    }
    db.AutoMigrate(&models.User{})
    log.Println("MySQL connected & migrated")

    // 连接 Redis
    rdb = redis.NewClient(&redis.Options{
        Addr: "localhost:6379",
        DB:   0,
    })
    // 可选：Ping 测试
    log.Println("Redis connected")

    // 创建 Gin 引擎
    r := gin.Default()

    // 健康检查
    r.GET("/health", func(c *gin.Context) {
        c.JSON(200, gin.H{"ok": true})
    })

    // 公开路由
    r.POST("/register", handlers.Register(db))
    r.POST("/login", handlers.Login(db))

    // 需要认证的路由组
    auth := r.Group("/api")
    auth.Use(middleware.AuthMiddleware())
    {
        auth.GET("/me", handlers.GetMe(db))
    }

    // 启动服务（默认监听 :8080，这里指定端口 8081）
    if err := r.Run(":8081"); err != nil {
        log.Fatal(err)
    }
}
```

### 7. 测试接口

#### 启动服务
```bash
cd services/auth
go run main.go
```

#### 测试健康接口
```powershell
curl.exe http://localhost:8081/health
```
预期输出：`{"ok":true}`

#### 测试注册接口
```powershell
curl.exe -X POST http://localhost:8081/register -H "Content-Type: application/json" -d "{\"username\":\"testuser\",\"password\":\"123456\"}"
```
预期返回成功信息（如 `{"id":1,"username":"testuser"}`）。

#### 测试登录接口（获取 token）
```powershell
curl.exe -X POST http://localhost:8081/login -H "Content-Type: application/json" -d "{\"username\":\"testuser\",\"password\":\"123456\"}"
```
返回包含 `token` 的 JSON。

#### 测试受保护接口 `/api/me`
```powershell
$token = "上一步获取的token"
Invoke-WebRequest -Uri http://localhost:8081/api/me -Method GET -Headers @{"Authorization"="Bearer $token"}
```
返回当前用户信息。

---

## 📝 补充说明与设计思路

- **为什么用 Gin**：原生 `net/http` 写起来较啰嗦，Gin 提供了路由分组、中间件、JSON 绑定等便利，提高开发效率，符合主流实践。
- **JWT 鉴权**：无状态、适合微服务。用户登录后得到 token，后续请求携带 token 即可，服务器无需保存 session。
- **分层结构**：
  - `models`：数据层，对应数据库表。
  - `handlers`：控制器层，处理 HTTP 请求和响应。
  - `middleware`：中间件层，处理横切关注点（如鉴权、日志）。
  - `utils`：工具函数（JWT 加解密、密码加密等）。
- **Redis 用途**：未来可缓存用户在线状态、基础信息等，减轻数据库压力。
- **密码加密**：目前暂未实现加密（仅明文存储），下一步应引入 `golang.org/x/crypto/bcrypt` 对密码进行哈希处理。

---

## ⚠️ 注意事项

- 确保 MySQL 和 Redis 容器已通过 `local_up.ps1` 启动，否则服务启动时会连接失败。
- 若端口被占用，可修改 `main.go` 中的 `:8081` 为其他端口。
- 测试时若返回 401，请检查 token 是否正确或是否已过期（JWT 默认过期时间建议设为 24h）。
- 后续应将数据库密码、JWT 密钥等敏感信息移入环境变量或配置文件，避免硬编码。

---

## 📌 下一步计划

- 引入密码加密（bcrypt）。
- 完善错误处理与日志。
- 为 Redis 添加缓存逻辑（如存储用户 Token 黑名单或在线状态）。
- 编写单元测试。
- 将配置抽离到 `config` 包或使用 Viper 管理。

---

**今日进展已全部记录，代码已通过 `git add`、`commit`、`push` 同步至 GitHub。**

--- WIKI PAGE: 📆-2026.03.02-开发笔记.md ---

---

# 📆 2026.03.02 开发笔记：重构数据库层、引入缓存与状态同步雏形

## 🎯 今日目标：让代码更清晰，为高并发打下基础

今天的核心工作是**重构项目结构**，并引入 **Redis 作为缓存层**，为后续的游戏状态同步做准备。我们不仅让代码更加模块化，还初步实现了用户的在线状态管理，并加固了 WebSocket 连接的安全性。

---

## ✅ 具体完成工作

### 1. 模块化重构：将数据库连接抽离到 `db/` 包
- 之前 `main.go` 里直接写了连接 MySQL 和 Redis 的代码，随着服务增多会变得臃肿。
- 现在创建了 `services/auth/db/` 文件夹，将数据库初始化逻辑封装成独立函数。
- **效果**：`main.go` 现在只负责“指挥”——调用 `db.InitMySQL()` 和 `db.InitRedis()`，拿到客户端实例后交给 Gin 路由。代码更清晰，便于单元测试和复用。

```go
// main.go 变得极其简洁
func main() {
    db.InitMySQL()
    db.InitRedis()
    // ... 路由注册
}
```

### 2. 引入 Redis 缓存层（双层存储架构）
- **为什么需要 Redis？**  
  在游戏后端，有些数据是“高频读写”且可以容忍短暂丢失的，比如用户在线状态、实时位置、房间列表。这些数据放在内存数据库（Redis）中，读写速度极快，能支撑高并发。
- **我们做了什么？**  
  在用户登录成功时，除了在 MySQL 中存储账号信息，还会在 Redis 中设置一个 key，例如 `user:online:{userID}`，值为 `1` 并附带过期时间（如 10 分钟）。这样就能快速判断用户是否在线。
- **技术实现**：  
  在 `handlers/login.go` 中，登录验证成功后添加一行：
  ```go
  rdb.Set(ctx, "user:online:"+strconv.Itoa(int(user.ID)), 1, 10*time.Minute)
  ```

### 3. 状态同步雏形：WebSocket 连接时读取在线状态
- 我们在 `services/gateway` 或相关位置实现了 WebSocket 连接处理。
- 当客户端通过 WebSocket 连接时，会携带一个 `token` 参数（从 URL Query 中获取）。
- 服务器解析 token 得到用户 ID，然后去 Redis 查询 `user:online:{userID}`，如果存在且值为 1，则认为用户已在线，允许建立连接；否则拒绝。
- **效果**：初步实现了“在线用户列表”的概念，为后续的实时消息推送打下基础。

### 4. 安全连接：WebSocket 握手时进行 token 验证
- 之前 WebSocket 可能是完全开放的，任何人都能连接。
- 现在我们在 WebSocket 升级前的 HTTP 握手阶段，通过 `c.Query("token")` 获取 token，验证其合法性（解析 JWT），并从 Redis 检查在线状态。
- 只有合法且未过期的 token 才能升级为 WebSocket 连接。
- **好处**：防止未授权用户占用连接资源，确保每个连接都对应一个真实登录的用户。

```go
// 伪代码：WebSocket 路由处理
func wsHandler(c *gin.Context) {
    token := c.Query("token")
    userID, err := utils.ParseToken(token)
    if err != nil {
        c.String(401, "unauthorized")
        return
    }
    // 检查 Redis 在线状态
    val, _ := rdb.Get(ctx, "user:online:"+userID).Result()
    if val != "1" {
        c.String(403, "not online")
        return
    }
    // 升级为 WebSocket 连接
    // ...
}
```

---

## 🧠 技术要点解析（知其所以然）

### 1. 架构演进：从“面条式”到“模块化”
- **之前**：所有代码挤在 `main.go`，连接逻辑和业务逻辑混在一起，难以维护和扩展。
- **现在**：将数据库连接、模型定义、处理函数、工具函数分层存放，符合**关注点分离**原则。这是走向微服务架构的第一步。

### 2. 双层存储：MySQL + Redis 的经典搭配
- **MySQL**：负责持久化存储，保存用户账号、装备、等级等不可丢失的数据。
- **Redis**：负责高速缓存，保存在线状态、实时位置、临时数据。读写速度可达微秒级，支持高并发。
- 这种架构在游戏、社交、电商等领域广泛应用，能显著降低数据库压力。

### 3. 状态同步雏形
- 通过在 Redis 中标记用户在线状态，我们实现了最简单的状态同步：其他服务（如匹配服务）可以查询某个用户是否在线。
- 后续可以扩展为存储用户当前所在房间、游戏状态等，实现真正的实时同步。

### 4. 安全连接：token 验证的 WebSocket
- WebSocket 是长连接，如果被恶意利用会消耗大量服务器资源。通过在握手时验证 token，我们确保了每个连接都经过身份认证。
- 结合 JWT 的无状态特性，即使服务器集群也能轻松验证。

---

## 📁 项目结构更新（涉及部分）

```
services/auth/
├── db/
│   ├── mysql.go          # MySQL 连接初始化
│   └── redis.go          # Redis 连接初始化
├── models/
│   └── user.go
├── handlers/
│   ├── register.go
│   ├── login.go          # 登录成功后写入 Redis 在线状态
│   └── user.go
├── middleware/
│   └── auth.go
├── utils/
│   └── jwt.go
└── main.go               # 简洁版本
```

---

## 🚀 下一步计划

1. **完善 Redis 数据结构**：用 Hash 存储更多在线信息（如进入的游戏房间ID、最后心跳时间）。
2. **实现心跳机制**：客户端定期发送心跳，服务器更新 Redis 中的过期时间，防止在线状态过期。
3. **广播系统**：当用户上线/下线时，通过 Redis Pub/Sub 通知其他服务（如匹配服务、聊天服务）。
4. **单元测试**：为数据库层和 handler 编写测试，确保重构不影响原有功能。

---

## 📝 今日总结

今天我们完成了代码结构的**模块化重构**，引入了 Redis 作为缓存层，并实现了**基于 Redis 的在线状态管理**和**WebSocket 安全连接验证**。这些改进让我们的后端架构更贴近生产环境的高并发游戏后端，也为后续实现实时匹配、聊天等功能铺平了道路。每一步都理解了“为什么这么做”，而不是盲目写代码，这比单纯的功能实现更有价值。

**代码已通过 `git add/commit/push` 同步至 GitHub，本次提交信息：`refactor: 抽离数据库连接，增加 Redis 在线状态及 WebSocket 鉴权`**。

---

--- WIKI PAGE: 📆-2026.03.02.md ---

---

# 📆 2026.03.02 下午开发笔记：实现 WebSocket 连接管理器（Hub）与全服广播

## ✅ 今日下午完成工作

### 1. 创建连接管理器 `hub.go`
- 在 `services/auth/handlers/` 下新建 `hub.go`。
- 定义 `Client` 结构体，存储用户 ID 和 WebSocket 连接。
- 定义 `Hub` 结构体，内含 `sync.Map` 保证并发安全，用于存储所有在线连接。
- 提供三个核心方法：
  - `Register(userID int, conn *websocket.Conn)`：玩家上线时登记。
  - `Unregister(userID int)`：玩家下线时注销。
  - `Broadcast(message []byte)`：向所有在线玩家广播消息。
- 声明全局变量 `GlobalHub` 便于各处调用。

### 2. 改造 `websocket.go` 接入 Hub
- 修改 `HandleWS` 函数，在成功升级连接后：
  - 调用 `GlobalHub.Register(userID, conn)` 登记上线。
  - 使用 `defer` 确保连接关闭时调用 `GlobalHub.Unregister(userID)` 注销下线。
- 修改消息循环：将原本的“回音壁”改为“世界聊天”，每当收到消息，调用 `GlobalHub.Broadcast()` 将消息发送给所有在线玩家。
- 日志中加入用户 ID，便于追踪。

### 3. 测试双开聊天（模拟两个玩家）
- 注册第二个测试账号 `me`（密码 `123`），获取 Token。
- 在 Postman 中打开两个 WebSocket 连接窗口：
  - 窗口 1：`ws://localhost:8081/ws?token=<testuser的Token>`
  - 窗口 2：`ws://localhost:8081/ws?token=<me的Token>`
- 测试结果：
  - 窗口 1 发送消息，窗口 2 立即收到；窗口 2 发送消息，窗口 1 也立即收到。
  - 终端日志正确显示玩家 ID 和消息内容。
- **成功实现多玩家实时通信的基础设施**。

---

## 📁 文件变更清单

| 文件路径 | 变更说明 |
|----------|----------|
| `services/auth/handlers/hub.go` | 新增文件，实现 Hub 结构体及方法 |
| `services/auth/handlers/websocket.go` | 引入 Hub，增加上线/下线登记，消息改为广播 |
| (其他文件如 `main.go` 无需修改) | - |

---

## 🧪 测试验证
- Postman 中两个 WebSocket 窗口同时连接并互相发送消息。
- 服务端终端输出显示玩家 ID 及广播日志。

---

## 📝 Git 提交信息（中英对照）

在项目根目录执行：

```bash
git add .
git commit -m "feat(websocket): implement connection hub for real-time broadcast

- Add hub.go with Client, Hub structs and thread-safe sync.Map
- Register/Unregister WebSocket connections in GlobalHub
- Replace echo logic with Broadcast to all online clients
- Test dual-client chat successfully with two accounts

中英对照：
- 新增 hub.go，包含 Client、Hub 结构体及线程安全的 sync.Map
- 在 GlobalHub 中登记/注销 WebSocket 连接
- 将回音壁逻辑改为向所有在线客户端广播
- 使用两个账号测试双客户端聊天成功"

git push origin main
```

---

## 🎯 技术要点总结

- **连接管理**：使用 `sync.Map` 存储所有在线连接，避免并发读写 panic。
- **全局单例**：`GlobalHub` 作为全局变量，任何地方都能访问在线列表。
- **广播机制**：遍历所有连接发送消息，是实现聊天、公告、状态同步的基础。
- **安全与状态**：每个连接都与已验证的用户 ID 绑定，杜绝匿名连接。

---

## 🚀 下一步规划

你已经具备了：
- 用户认证（JWT）
- 在线状态（Redis）
- 实时通信（WebSocket + Hub）

接下来可以：
1. 实现私聊（根据目标用户 ID 定向发送）。
2. 房间/匹配系统（将玩家分组，组内广播）。
3. 心跳检测与断线重连。

**今日成果已通过 Git 记录，Day 4 完美收官！**

--- WIKI PAGE: 📆-2026.03.03.01-上午开发笔记.md ---

---

# 📆 2026.03.03.01 上午开发笔记：数据库解耦、消息持久化与模型扩展

## ✅ 今日上午完成工作

1. **数据库连接与迁移逻辑解耦**  
   - 将 MySQL 连接初始化、`AutoMigrate` 等操作从 `main.go` 完全迁移至 `db` 包，实现职责分离，`main.go` 更加简洁。

2. **全局数据库实例管理**  
   - 采用全局变量模式（如 `handlers.DB`）统一管理数据库实例，消除在各 Handler 函数间透传 `*gorm.DB` 对象，简化代码调用。

3. **WebSocket 聊天消息持久化**  
   - 在 WebSocket 消息循环中，将玩家发送的每条聊天记录异步写入 MySQL 数据库，实现消息的可追溯与历史存储。

4. **新增 `PlayerPosition` 模型并迁移**  
   - 定义 `PlayerPosition` 结构体，用于后续存储玩家实时位置（坐标、地图等），并执行数据库迁移，为位置同步功能奠定基础。

---

## 📁 文件变更清单（推测）

| 文件路径 | 变更说明 |
|----------|----------|
| `services/auth/db/mysql.go` | 新增或扩展，包含 MySQL 连接初始化与自动迁移逻辑 |
| `services/auth/main.go` | 移除 MySQL 初始化细节，仅调用 `db.InitMySQL()` |
| `services/auth/handlers/websocket.go` | 在消息广播逻辑中，调用数据库写入聊天记录 |
| `services/auth/handlers/base.go`（新建） | 定义全局变量 `var DB *gorm.DB` 供 handlers 使用 |
| `services/auth/models/player.go` | 新增模型定义 |


---

## 📝 Git 提交信息（中英对照）

在项目根目录执行：

```bash
git add .
git commit -m "refactor: database modularization and implement chat message persistence

- Decouple MySQL connection and AutoMigrate from main to db package
- Introduce global DB instance to eliminate object passing in handlers
- Persist chat messages to MySQL during WebSocket broadcast
- Add PlayerPosition model and migrate for future location sync

中英对照：
- 将 MySQL 连接与自动迁移从 main 解耦到 db 包
- 引入全局数据库实例，消除 handler 间的对象透传
- 在 WebSocket 广播时将聊天消息持久化到 MySQL
- 新增玩家位置模型并迁移，为位置同步做准备"

git push origin main
```

---

## 🧠 技术要点总结

- **解耦与职责分离**：数据库初始化细节移出 `main`，有利于单元测试和模块复用。
- **全局变量模式**：在小型项目中，通过 `handlers.DB` 全局变量简化调用链，避免层层传递，提高开发效率（注意并发安全，GORM 的 DB 对象本身是协程安全的）。
- **消息持久化**：将实时聊天数据落地到 MySQL，可用于历史记录查询、敏感信息审计等场景，是完整 IM/游戏系统的必备功能。
- **模型扩展**：新增 `Player` 为后续实时位置同步（如移动、战斗）打下基础，体现系统演进的前瞻性。

---

## 🚀 下一步预告

完成了数据库解耦、消息持久化和位置模型扩展后，接下来可以：

1. **实现位置同步逻辑**：在 WebSocket 中接收玩家位置更新，广播给同地图/房间的玩家。
2. **引入 Redis 实时位置缓存**：降低 MySQL 读写压力，提升位置查询性能。
3. **设计房间/匹配系统**：将玩家分组，实现组内广播而非全服广播。

**今日上午的优化让项目架构更清晰，同时为实时性功能添加了数据支撑，继续保持！**

---

--- WIKI PAGE: 📆-2026.03.03.03-下午开发笔记B.md ---

📆 2026.03.03.03 下午开发笔记B
A. 实现 JSON 协议分流与位置同步
升级 WebSocket 通信为 JSON 格式，用 type 字段区分 chat / move。

聊天：消息存 MySQL，全服广播 JSON。

移动：更新 PlayerPosition 表（Upsert），全服广播坐标。

新玩家上线时推送 世界状态快照，断开时广播下线通知。

B. 心跳机制与进度盘点
心跳：定期 Ping/Pong 检测僵尸连接，后续集成。

已具备：JWT 鉴权、全局 Hub、位置/聊天双向同步、上线快照、离线清理。

📌 Git Commit 建议
text
feat: 实现 WebSocket JSON 协议、位置/聊天同步及上线快照

--- WIKI PAGE: 📆-2026.03.04-开发笔记：Unity-对接与问题修复.md ---

---

# 📆 2026.03.04 开发笔记：Unity 对接与问题修复

## ✅ 今日核心进展

### 1. 后端（Go）优化
- **JWT 有效期延长**：从 24h 调整为 **72h**，减少开发中重复登录。
- **密钥统一**：确保 `login` 处理器与 `jwt` 工具包使用一致的密钥，避免掉线。
- **日志优化**：调整登录日志输出，保持与原始架构一致。

### 2. 前端（Unity）联调与修复
- **DLL 依赖修复**：导入 `System.Runtime.CompilerServices.Unsafe.dll`，解决 Protobuf 运行时异常。
- **脚本冲突解决**：修正 `NetManager` 脚本重复挂载导致的频繁断线问题。
- **平滑移动实现**：将方块位移从“瞬移”改为基于 `Vector3.MoveTowards` 的平滑同步。
- **单例保护**：增加单例模式，防止场景中出现多个网络管理实例。

## 📌 Git 提交信息

### 后端（Go）
```bash
feat(auth): extend JWT expiration to 72h and unify secret key

- 修改 utils/jwt.go 将 Token 有效期从 24h 延长至 72h 以方便开发调试
- 确保 login 处理函数与 jwt 工具包使用统一的密钥 (your-secret-key)
- 优化了登录日志输出，保持与原始架构一致
```

### 前端（Unity）
```bash
fix(network): resolve DLL dependency and implement smooth movement

- 导入 System.Runtime.CompilerServices.Unsafe.dll 修复 Protobuf 运行异常
- 修正 NetManager 脚本重复挂载导致的频繁断线问题
- 将方块位移逻辑从“瞬移”改为基于 Vector3.MoveTowards 的平滑同步
- 增加单例保护，防止场景中出现多个网络管理实例
```

## 🧠 技术要点总结
- **密钥一致性**：前后端 JWT 密钥必须完全一致，否则 token 验证失败导致连接断开。
- **Protobuf 依赖**：C# 环境需手动补全 `System.Runtime.CompilerServices.Unsafe` 等底层库。
- **单例管理**：网络管理器应保证全局唯一，避免重复连接和资源冲突。
- **平滑移动**：游戏表现上，使用插值算法（如 `MoveTowards`）替代直接赋值，提升视觉体验。

## 🚀 下一步建议
- 实现多玩家同步：在 Unity 中实例化多个方块，根据后端广播的坐标更新各自位置。
- 添加简单 UI：显示在线玩家列表或聊天框。
- 压力测试：模拟多客户端连接，观察后端性能。

---

--- WIKI PAGE: 📆-2026.03.08-开发笔记.md ---

---

# 📆 2026.03.08 开发笔记：多客户端同步闭环与工程清理

## ✅ 今日核心进展

### 1. 后端 (Go) 状态
- 稳定运行于 WSL 环境，正确校验 JWT Token。
- 实时广播玩家坐标，支撑多客户端位置同步。

### 2. 网络模块 (Unity) 重构
- `NetManager` 恢复为核心组件，支持 WebSocket 二进制流（Protobuf）收发。
- 增加单例保护，确保全局唯一实例。

### 3. 移动逻辑实现
- **本地玩家**：基于 `Vector3.MoveTowards` 实现平滑滑行，附带转向逻辑。
- **远程玩家**：通过 `Dictionary` 动态生成和跟踪，实时显示位置（当前为瞬移，后续可优化插值）。

### 4. 联机测试验证
- **环境**：Standalone Build（玩家1） + Unity Editor（玩家2） 双端互连。
- **结果**：双方可见对方位置，多客户端同步逻辑闭环。

### 5. 工程清理
- 删除无效的 `.bat` 脚本和废弃 UI 脚本。
- 消除所有 Inspector 面板的 "Missing Script" 警告。

## 📌 Git 提交信息
```bash
feat(network): implement multi-client synchronization and local smooth movement

Details:
* Integrated WebSocket-based position synchronization using Protobuf.
* Implemented local player smooth movement using Vector3.MoveTowards.
* Enabled dynamic spawning and tracking of remote players in a Dictionary.
* Verified multi-instance connectivity between standalone build and Unity Editor.
* Cleaned up deprecated UI scripts and resolved "Missing Script" warnings.
```

## ⚠️ 注意事项
- 当前玩家 ID 配置：
  - 打包版 EXE 使用 **ID=1**
  - Unity 编辑器内请将 **My User Id** 改为 **2**，避免 ID 冲突导致无法互见。

## 🧠 技术要点总结
- **多客户端同步闭环**：从后端广播到前端解析、生成/跟踪远程玩家，全链路打通。
- **平滑移动**：本地移动使用插值算法（`MoveTowards`），远程移动暂为瞬移（可后续加入插值）。
- **二进制协议**：Protobuf 在 Unity 端落地，依赖手动补全的 `System.Runtime.CompilerServices.Unsafe.dll`。
- **工程健康度**：清理冗余脚本和警告，项目结构更干净。

## 🚀 当前状态
- 后端启动（WSL 手动运行）
- EXE 启动（玩家1）
- Unity 启动（玩家2）
- 双方互见，同步稳定 ✅

**下一个里程碑：为远程玩家加入移动插值，消除瞬移感，提升视觉体验。**

---

--- WIKI PAGE: 📆-2026.03.17-开发笔记：架构跃迁——拆分三仓库与微服务网关升级.md ---

# 📆 2026.03.17 开发笔记：架构跃迁——拆分三仓库与微服务网关升级

## ✅ 今日核心变革

项目从单体仓库拆分为三个独立的代码仓库，实现职责彻底分离：

1. **React Web 前端大厅**（`asternova-web-client`）  
2. **Unity 核心战斗引擎**（`MyGameDemo_Client`）  
3. **Go 微服务后端**（`game-backend-demo`）

---

## 📌 Git 提交信息（按仓库分别提交）

### 1. React Web 前端大厅 (`asternova-web-client`)

```plaintext
feat(architecture): implement SPA web client with React and Unity WebGL bridge

- 初始化 Next.js + Tailwind + shadcn 现代前端架构。
- 完成 Login/Register 鉴权模块及 JWT 浏览器持久化。
- 构建赛博风大厅，实现 Web 端 WebSocket 匹配与职业选择。
- 集成 react-unity-webgl，实现 WebGL 容器挂载与全屏赛博加载过渡。
- 建立跨界通信桥梁（The Bridge），成功将对局参数注入 Unity 引擎。
```

### 2. Unity 核心战斗引擎 (`MyGameDemo_Client`)

```plaintext
refactor(core): execute 'The Great Decoupling' to strip UI and integrate WebGL

- 彻底移除过时的 UI Toolkit (UXML/USS) 依赖及相关控制器（Login/Lobby Controller）。
- 重构 GameManager，暴露 `EnterBattle(payload)` 接口供外部 Web 宿主调用。
- 升级 NetManager 网络入口，新增 `ConnectToArena()`。
- 支持基于 URL 参数（token, room_id）的 WebSocket 房间直连机制。
- 将 Unity 引擎定位降级为纯粹的 3D 渲染与战斗同步容器。
```

### 3. Go 微服务后端 (`game-backend-demo`)

```plaintext
feat(gateway): upgrade WebSocket hub for dual-protocol (JSON/Protobuf) routing

- 重构网关层消息读写逻辑，实现根据 messageType 的智能分流。
- 匹配大厅阶段（React端）：全面采用 JSON 文本协议进行握手与搓合推送。
- 战斗核心阶段（Unity端）：保持高频 Protobuf 二进制协议进行帧同步。
- 修复 Matcher 搓合成功后的下发格式异常，确保 Web 宿主正确解析 `match_success`。
```

---

## 🌌 架构跃迁总结（The Great Decoupling）

> **"Reach Beyond the Stars"** —— 这次重构真正实现了质的飞跃。

- **告别 UI 泥潭**：此前（03.13）还在 Unity 中与严苛的 XML 模式、字体挂载、CSS 塌陷作斗争，如今通过 React + Tailwind 一行 `bg-gradient-to-br from-pink-300 to-purple-500` 即可优雅复现渐变色彩体系。
- **彻底的职责分离**：Unity 不再背负界面负担，退居为纯粹的 60 帧 3D 渲染与战斗同步引擎；React 则接管了完美的 SPA 体验。
- **双语枢纽诞生**：Go 后端升级为双协议网关，既能用 JSON 与 Web 前端“谈笑风生”，又能用 Protobuf 与 Unity 引擎“硬核同步”，为微服务化打下坚实基础。

---

--- WIKI PAGE: 🗓️2026.02.25-学习笔记.md ---

Welcome to the my-backend-demo wiki!
# ## ### **_``_**
# 🗓️ 2026.02.25 学习笔记：Git + GitHub 环境搭建与基础操作

## 🎯 今日目标
- 搭建本地开发环境（VS Code、Go、Git、WSL）
- 注册并配置 GitHub 账号
- 掌握 Git 基础命令，实现本地代码与远程仓库的同步

---

## ⚙️ 一、环境搭建

| 工具 | 用途 | 安装方式 |
|------|------|----------|
| **VS Code** | 代码编辑器 | 官网下载安装，安装后配置 Go 插件 |
| **Go语言** | 后端开发语言 | 官网下载安装包，配置环境变量，VS Code 安装 Go 插件及工具（如 gopls、dlv 等） |
| **Git** | 版本控制工具 | 官网下载安装，安装后配置用户信息（`git config --global user.name/email`） |
| **WSL** | Windows 下的 Linux 子系统 | PowerShell 执行 `wsl --install`，安装后更新内核（`wsl --update`），获得 Linux 环境支持 |

> ✅ **注意**：WSL 安装后可以在 Windows 上直接使用 Linux 命令行，方便后续开发。

---

## 🐙 二、GitHub 配置

1. **注册账号**：在 [GitHub](https://github.com) 完成注册，用户名 `TimeCraker`。
2. **创建仓库**：
   - 点击右上角 `+` → **New repository**
   - 仓库名：`my-backend-demo`
   - 选择 Public，**不勾选** “Add a README”（本地已有 README，避免冲突）
3. **生成 SSH 密钥并添加到 GitHub**（可选，但推荐）：
   ```bash
   ssh-keygen -t ed25519 -C "你的邮箱"
   ```
   将公钥（`~/.ssh/id_ed25519.pub`）内容添加到 GitHub 的 **Settings → SSH and GPG keys** 中。

---

## 🔗 三、本地仓库与远程仓库关联

1. **在本地创建项目文件夹并初始化为 Git 仓库**
   ```bash
   cd Desktop
   mkdir my-backend-demo
   cd my-backend-demo
   git init
   ```

2. **添加远程仓库地址（以 HTTPS 为例）**
   ```bash
   git remote add origin https://github.com/TimeCraker/my-backend-demo.git
   ```
   验证：`git remote -v`

3. **拉取远程已有文件（如有）并合并**
   ```bash
   git pull origin main --allow-unrelated-histories
   ```
   （如果远程有 README 或 .gitignore，会合并到本地）

4. **添加本地文件并首次推送**
   ```bash
   echo "# My Backend Demo" > README.md
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```

---

## 🔁 四、日常 Git 工作流（重要）

每天开始工作前和结束后，遵循以下步骤：

```bash
# 1. 开始写代码前：同步远程最新版本（避免冲突）
git pull

# 2. 编写/修改代码（在本地进行）

# 3. 完成后：将改动添加到暂存区
git add .

# 4. 提交到本地仓库，并附上清晰说明
git commit -m "本次修改的简要描述"

# 5. 推送到 GitHub 远程仓库
git push
```

> ✅ **注意**：
> - `git add .` 会添加当前目录下所有改动（包括新增、修改、删除）。如果有不需要追踪的文件（如编译产物），务必创建 `.gitignore` 文件并在其中列出。
> - 首次推送时使用 `-u` 参数建立本地分支与远程分支的关联，之后只需 `git push`。
> - 如果在 `git push` 时被拒绝（提示远程有更新），先执行 `git pull` 合并，再重新 `push`。

---
## 把Ubuntu操作系统下好了 然后下载好了Docker Desktop

---

## 🧾 今日总结
今天完成了从零到一的飞跃：搭建了开发环境、注册 GitHub、创建仓库、掌握 Git 基础命令，并成功将本地代码推送到了云端。
你已经拥有了一个可靠的代码备份和版本管理工具，接下来可以专注于写代码了！每天结束时用 `git add/commit/push` 记录你的成果，GitHub 会自动生成你的开发历程。

---

# 📆 2026.02.25 极简笔记（省流版）

## ✅ 今日完成
- **VS Code** + **Go** 安装，VS Code 配置 Go 插件。
- **Git** 安装，**GitHub** 账号创建。
- **WSL** 安装并更新（`wsl --install` → `wsl --update`），获得 Linux 子系统。
- **Docker Desktop** 下载（待安装）。
- **Git 基础**：
  - 创建 GitHub 仓库（`my-backend-demo`，Public，不勾 README）。
  - 本地初始化：`git init`
  - 关联远程：`git remote add origin https://github.com/TimeCraker/my-backend-demo.git`
  - 首次合并：`git pull origin main --allow-unrelated-histories`
  - 首次推送：`git add .` → `git commit -m "init"` → `git push -u origin main`

## 🔁 日常 Git 流程（每天必记）
```bash
# 开工前
git pull

# 写代码...

# 收工
git add .
git commit -m "简明描述"
git push
```

## 📝 笔记保存
- **Wiki**：点击仓库导航栏 `Wiki` → `New Page`，标题用日期（如 `2026-02-25`），保存即可。
- **本地文件**：仓库内新建 `NOTES.md`，用 Markdown 写，然后 `add/commit/push`。

## ⚠️ 注意
- 首次推送用 `-u` 建立关联，之后只需 `git push`。
- 如果 `push` 被拒绝，先 `git pull` 再 `push`。
- 用 `.gitignore` 忽略临时文件（如 `.exe`、`node_modules/`）。
- **Docker Desktop** 安装后即可开始容器化学习。

---
